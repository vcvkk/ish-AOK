build/
*.o
.cache/
xcuserdata/
.floo

cross-*.txt

node_modules
app/xtermjs/xterm-dist
app/xtermjs/.cache

subprojects/
!subprojects/*.wrap

fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots
fastlane/test_output

e2e_out/
root.tar.gz
foo.tgz
alpine-minirootfs-*.tar.*
devuan-minirootfs-*.tar.*

.tmp*
com.apple.DeveloperTools/
tests/host/
diagnostics*
# DiagnosticsBridge.h is a real build dependency (included by kernel/calls.c for
# both the CLI and app builds), not generated diagnostics output. The pattern
# above matches it case-insensitively on macOS, so a fresh checkout silently
# lacked it and the build broke. Keep it tracked.
!app/DiagnosticsBridge.h

.claude/
CLAUDE.md
.clangd
.devcontainer/
.prettierrc
.vscode/
tsconfig.json

Alpine3.23.3(x86_64).tar.gz
Alpine3.23.3_strace.tar.gz
Devuan5(Debian12) 2.tar.gz

.clinerules
.cursor/
.gemini/
.windsurfrules
AGENTS.md
opencode.json
